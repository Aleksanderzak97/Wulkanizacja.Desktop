<UserControl x:Class="Wulkanizacja.User.Views.GeneralView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Wulkanizacja.User.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Search Panel -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="5">
            <TextBox Width="200" Margin="5"/>
            <Button Content="Search" Margin="5"/>
        </StackPanel>

        <!-- List -->
        <ListView Grid.Row="2" Margin="5" ItemsSource="{Binding Items}" Background="DarkGray">
            <ListView.View>
                <GridView>
                    <GridViewColumn Header="Marka" DisplayMemberBinding="{Binding Property1}" Width="Auto"/>
                    <GridViewColumn Header="Model" DisplayMemberBinding="{Binding Property2}" Width="Auto"/>
                    <GridViewColumn Header="Rozmiar" Width="Auto"/>
                    <GridViewColumn Header="Sezon" Width="Auto"/>
                    <GridViewColumn Header="Nośność" Width="Auto"/>
                    <GridViewColumn Header="Indeks prędkości" Width="Auto"/>
                    <GridViewColumn Header="Indeks nośności" Width="Auto"/>
                    <GridViewColumn Header="Rok produkcji" Width="Auto"/>
                    <GridViewColumn Header="Ilość sztuk" Width="Auto"/>
                </GridView>
            </ListView.View>
        </ListView>
    </Grid>
</UserControl>
